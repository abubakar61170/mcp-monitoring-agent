import sys
import requests
import langchain
import langgraph

print(f"Python Version: {sys.version}")
print(f"LangChain Version: {langchain.__version__}")
print("Environment setup successful! Ready to build the agent.")