{
  "title": "Infra CPU Live",
  "panels": [{
    "title": "Container CPU Usage",
    "type": "timeseries",
    "targets": [{
      "expr": "rate(container_cpu_usage_seconds_total[5m])*100",
      "legendFormat": "{{container_name}}",
      "refId": "A"
    }],
    "gridPos": {"h": 10, "w": 24, "x": 0, "y": 0}
  }],
  "schemaVersion": 39
}
